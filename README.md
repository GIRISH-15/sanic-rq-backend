# sanic-rq-backend

Quick start:
1. Install Docker & Docker Compose
2. Clone repo and run: docker compose up -d --build
3. Health: curl http://localhost:8000/health

Do NOT commit .env or keys. See ops/ for deployment scripts.
